name: 'Setup Talenta'
description: 'Setup Talenta CLI'
author: 'el7cosmos'
inputs:
  milliseconds: # change this
    required: true
    description: 'input description here'
    default: 'default value if applicable'
runs:
  using: 'node12'
  main: 'dist/index.js'
